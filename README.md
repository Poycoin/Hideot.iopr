# Hideot.iopr

A minimal project scaffold for the Hideot.iopr repository.

## Overview

This repository is initialized as a basic Python package so it can be imported, tested, and run as a CLI from the project root.

## Usage

```bash
python -m hideot
python -m pytest
```
